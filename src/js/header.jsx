/** @jsx React.DOM */
var React = require('react');
var Header = React.createClass({
    render: function() {
        return (
            <header>
                Keep
            </header>
        );
    },
});

module.exports = Header;
